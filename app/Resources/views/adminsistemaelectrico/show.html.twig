{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminSistemaElectrico</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminSistemaElectrico.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminSistemaElectrico.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipo_sistema_electrico_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipo_sistema_electrico_edit', { 'id': adminSistemaElectrico.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
