{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Sistema Eléctrico</div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminSistemaElectrico in adminSistemaElectricos %}
                <tr>
                    <td>{{ adminSistemaElectrico.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurables_tipo_sistema_electrico_edit', { 'id': adminSistemaElectrico.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <a class="btn btn-primary" href="{{ path('configurables_tipo_sistema_electrico_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo Sistema Electrico</a>

{% endblock %}
