{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoCondicionTerreno</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoCondicionTerreno.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoCondicionTerreno.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admintipocondicionterreno_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admintipocondicionterreno_edit', { 'id': adminTipoCondicionTerreno.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
