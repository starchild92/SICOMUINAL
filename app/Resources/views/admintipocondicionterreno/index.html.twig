{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Tipo de Ayuda Especial</div>
        <table class="table-bordered table table-stripped">
            <thead>
                <tr>
                    <th>Condición</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminTipoCondicionTerreno in adminTipoCondicionTerrenos %}
                <tr>
                    <td>{{ adminTipoCondicionTerreno.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('admintipocondicionterreno_edit', { 'id': adminTipoCondicionTerreno.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a class="btn btn-primary" href="{{ path('admintipocondicionterreno_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo</a>
{% endblock %}
