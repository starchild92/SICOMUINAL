{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoPadecencia</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoPadecencium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoPadecencium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_padecencia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_padecencia_edit', { 'id': adminTipoPadecencium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
