{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminUbicacionTrabajo</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminUbicacionTrabajo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminUbicacionTrabajo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_ubicacion-trabajo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_ubicacion-trabajo_edit', { 'id': adminUbicacionTrabajo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
