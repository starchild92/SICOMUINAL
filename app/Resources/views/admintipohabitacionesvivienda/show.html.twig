{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoHabitacionesVivienda</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoHabitacionesVivienda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoHabitacionesVivienda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_habitaciones_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_habitaciones_edit', { 'id': adminTipoHabitacionesVivienda.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
