{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoVivienda</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoVivienda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoVivienda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_vivienda_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_vivienda_edit', { 'id': adminTipoVivienda.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
