{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Capacidad de Bombona</div>
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminCapacidadBombona in adminCapacidadBombonas %}
                <tr>
                    <td>{{ adminCapacidadBombona.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('admincapacidadbombona_edit', { 'id': adminCapacidadBombona.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <a class="btn btn-primary" href="{{ path('admincapacidadbombona_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo Proveedor de Gas</a>
{% endblock %}
