{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminCapacidadBombona</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminCapacidadBombona.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminCapacidadBombona.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admincapacidadbombona_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admincapacidadbombona_edit', { 'id': adminCapacidadBombona.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
