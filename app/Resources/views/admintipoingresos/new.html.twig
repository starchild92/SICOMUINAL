{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoIngresos creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Agregar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('configurable_tipos_ingresos_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
