{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoIngresos</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoIngreso.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoIngreso.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipos_ingresos_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipos_ingresos_edit', { 'id': adminTipoIngreso.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
