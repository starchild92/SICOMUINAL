{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoTelefonia</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoTelefonium.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoTelefonium.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipo_telefonia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipo_telefonia_edit', { 'id': adminTipoTelefonium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
