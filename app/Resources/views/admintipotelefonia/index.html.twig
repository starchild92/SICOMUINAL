{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Tipo Telefonía</div>
        <table class="table-striped table table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminTipoTelefonium in adminTipoTelefonias %}
                <tr>
                    <td>{{ adminTipoTelefonium.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurables_tipo_telefonia_edit', { 'id': adminTipoTelefonium.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>        
    <a class="btn btn-primary" href="{{ path('configurables_tipo_telefonia_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nueva Telefonía</a>
{% endblock %}
