{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoGas creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('configurables_tipos_gas_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
