{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoMascotas</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoMascota.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoMascota.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_mascotas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_mascotas_edit', { 'id': adminTipoMascota.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
