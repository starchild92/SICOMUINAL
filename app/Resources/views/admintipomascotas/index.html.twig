{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Tipo de Mascotas</div>
        <table class="table-bordered table table-stripped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminTipoMascota in adminTipoMascotas %}
                <tr>
                    <td>{{ adminTipoMascota.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurable_tipo_mascotas_edit', { 'id': adminTipoMascota.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a class="btn btn-primary" href="{{ path('configurable_tipo_mascotas_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo</a>
{% endblock %}
