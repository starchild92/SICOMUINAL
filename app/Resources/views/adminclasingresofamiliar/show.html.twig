{% extends 'base.html.twig' %}

{% block body %}
    <h3 class="text-center">Clasificación Ingreso Familiar</h3>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminClasIngresoFamiliar.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminClasIngresoFamiliar.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_clasificacion_ingreso_familiar_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_clasificacion_ingreso_familiar_edit', { 'id': adminClasIngresoFamiliar.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
