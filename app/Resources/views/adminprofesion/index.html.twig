{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Profesiones</div>
        <table class="table-bordered table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminProfesion in adminProfesions %}
                <tr>
                    <td>{{ adminProfesion.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurable_profesiones_edit', { 'id': adminProfesion.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    
    <a class="btn btn-primary" href="{{ path('configurable_profesiones_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nueva Profesión</a>

{% endblock %}
