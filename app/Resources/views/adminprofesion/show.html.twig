{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminProfesion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminProfesion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminProfesion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_profesiones_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_profesiones_edit', { 'id': adminProfesion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
