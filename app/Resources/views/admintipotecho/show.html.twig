{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoTecho</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoTecho.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoTecho.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_techo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_techo_edit', { 'id': adminTipoTecho.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
