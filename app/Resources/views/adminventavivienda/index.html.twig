{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminVentaVivienda list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for adminVentaVivienda in adminVentaViviendas %}
            <tr>
                <td><a href="{{ path('configurable_venta_vivenda_show', { 'id': adminVentaVivienda.id }) }}">{{ adminVentaVivienda.id }}</a></td>
                <td>{{ adminVentaVivienda.nombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('configurable_venta_vivenda_show', { 'id': adminVentaVivienda.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('configurable_venta_vivenda_edit', { 'id': adminVentaVivienda.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_venta_vivenda_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
