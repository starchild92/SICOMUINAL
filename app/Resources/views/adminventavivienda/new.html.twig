{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminVentaVivienda creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('configurable_venta_vivenda_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
