{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminMisionesComunidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminMisionesComunidad.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminMisionesComunidad.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipos_misiones_comunidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipos_misiones_comunidad_edit', { 'id': adminMisionesComunidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
