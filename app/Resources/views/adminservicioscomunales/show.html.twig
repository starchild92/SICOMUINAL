{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminServiciosComunales</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminServiciosComunale.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminServiciosComunale.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipos_servicios_comunales_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipos_servicios_comunales_edit', { 'id': adminServiciosComunale.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
