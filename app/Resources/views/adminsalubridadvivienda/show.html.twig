{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminSalubridadVivienda</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminSalubridadVivienda.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminSalubridadVivienda.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_salubridad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_salubridad_edit', { 'id': adminSalubridadVivienda.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
