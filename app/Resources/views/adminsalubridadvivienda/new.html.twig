{% extends 'base.html.twig' %}

{% block body %}
    <h1>Salubridad Vivienda</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-primary btn-block" type="submit" value="Agregar Nuevo" />
    {{ form_end(form) }}
{% endblock %}
