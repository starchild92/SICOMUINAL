{% extends 'base.html.twig' %}

{% block body %}
    <h1>ParticipacionComunitaria edit</h1>

    {{ form_start(edit_form) }}
        {# {{ dump(edit_form) }} #}
        {% for elem in edit_form.preguntasParticipacionComunitaria.vars.AdminPreguntas  %}
            {{ dump(elem) }}
        {% endfor %}
        

        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('participacioncomunitaria_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
{% block javascripts %}
<script src="{{ asset('js/formOrganizaciones.js') }}"></script>
<script src="{{ asset('js/formPreguntas.js') }}"></script>
<script>
    $(document).ready(function() {
        $('.ui.dropdown').dropdown();
        $('.fluid.multiple.search.selection').dropdown({ allowAdditions: true });
    })
</script>
{% endblock %}