{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Tipo de Situación de Exclusion</div>
        <table class="table-bordered table table-stripped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Cantidad</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminTipoSituacionExclusion in adminTipoSituacionExclusions %}
                <tr>
                    <td>{{ adminTipoSituacionExclusion.situacion }}</td>
                    <td>{{ adminTipoSituacionExclusion.cantidad }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurable_tipo_situacion_exclusion_edit', { 'id': adminTipoSituacionExclusion.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a class="btn btn-primary" href="{{ path('configurable_tipo_situacion_exclusion_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo</a>
{% endblock %}
