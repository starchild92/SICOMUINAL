{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminRecoleccionBasura</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminRecoleccionBasura.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminRecoleccionBasura.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipo_recoleccion_basura_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipo_recoleccion_basura_edit', { 'id': adminRecoleccionBasura.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
