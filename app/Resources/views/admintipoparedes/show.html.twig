{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoParedes</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoParede.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoParede.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_paredes_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_paredes_edit', { 'id': adminTipoParede.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
