{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Crear Noticia</h1>
    <hr>
    {{ form_start(form, {'id':'appbundle_correo'}) }}
        <div class="row">
            <div class="col-sm-10">{{ form_row(form.titulo) }}</div>
            <div class="col-sm-2">{{ form_widget(form.visibilidad) }}</div>
        </div>
        {{ form_widget(form.cuerpo, {'id':'TypeHere'}) }}
        <hr class="little">
        <button class="btn btn-primary btn-block" type="submit"><i class="fa fa-save fa-fw"></i> Actualizar Texto del Correo </button>
    {{ form_end(form) }}

{% endblock %}
{% block javascripts %}
    <script src="http://cdn.tinymce.com/4/tinymce.min.js"></script>
    <script type="application/x-javascript">
        tinymce.init({selector:'#TypeHere'});
    </script>
{% endblock %}