{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminAguasServidas</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminAguasServida.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminAguasServida.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurables_tipos_aguas_servidas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurables_tipos_aguas_servidas_edit', { 'id': adminAguasServida.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
