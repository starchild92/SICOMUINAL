{% extends 'base.html.twig' %}

{% block body %}
    <h1>SituacionComunidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ situacionComunidad.id }}</td>
            </tr>
            <tr>
                <th>Preguntassituacioncomunidad</th>
                <td>{{ situacionComunidad.preguntasSituacionComunidad }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('situacioncomunidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('situacioncomunidad_edit', { 'id': situacionComunidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
