{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminPensionadoInstitucion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminPensionadoInstitucion.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminPensionadoInstitucion.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_pensionados_instituciones_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_pensionados_instituciones_edit', { 'id': adminPensionadoInstitucion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
