{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Preguntas Participación Comunitaria</div>
        <table class="table-striped table table-bordered">
            <thead>
                <tr>
                    <th>Interrogante</th>
                    <th>Respuesta</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminPreguntasParticipacionComunitarium in adminPreguntasParticipacionComunitarias %}
                <tr>
                    <td>{{ adminPreguntasParticipacionComunitarium.interrogante }}</td>
                    <td>{{ adminPreguntasParticipacionComunitarium.respuesta }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurables_preguntas_participacion_comunitaria_edit', { 'id': adminPreguntasParticipacionComunitarium.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <a class="btn btn-primary" href="{{ path('configurables_preguntas_participacion_comunitaria_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nueva Preguntas Participación Comunitaria</a>
{% endblock %}
