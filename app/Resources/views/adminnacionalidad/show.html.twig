{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminNacionalidad</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminNacionalidad.id }}</td>
            </tr>
            <tr>
                <th>Nacionalidad</th>
                <td>{{ adminNacionalidad.nacionalidad }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_nacionalidad_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_nacionalidad_edit', { 'id': adminNacionalidad.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
