{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoTenencia</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoTenencium.id }}</td>
            </tr>
            <tr>
                <th>Forma</th>
                <td>{{ adminTipoTenencium.forma }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_tenencia_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_tenencia_edit', { 'id': adminTipoTenencium.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
