{% extends 'base.html.twig' %}

{% block body %}
    <div class="panel panel-primary">
        <div class="panel-heading">Mecanismos de Información</div>
        <table class="table-striped table table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Opciones</th>
                </tr>
            </thead>
            <tbody>
            {% for adminMecanismoInformacion in adminMecanismoInformacions %}
                <tr>
                    <td>{{ adminMecanismoInformacion.nombre }}</td>
                    <td>
                        <a id="forModalEdit" href="{{ path('configurables_tipo_mecanismo_informacion_edit', { 'id': adminMecanismoInformacion.id }) }}"><i class="fa fa-edit"></i> Editar</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>        
    <a class="btn btn-primary" href="{{ path('configurables_tipo_mecanismo_informacion_new') }}" id="forModal"><i class="fa fa-plus"></i> Agregar Nuevo Mecanismo de Información</a>
{% endblock %}