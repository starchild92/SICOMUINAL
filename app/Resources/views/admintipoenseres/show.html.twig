{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdminTipoEnseres</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adminTipoEnsere.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ adminTipoEnsere.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('configurable_tipo_enseres_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('configurable_tipo_enseres_edit', { 'id': adminTipoEnsere.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
